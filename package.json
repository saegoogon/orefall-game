{
  "name": "orefall-idle",
  "version": "1.0.0",
  "private": true,
  "description": "Server-backed browser idle mining RPG with mock payments and leaderboard.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  }
}
